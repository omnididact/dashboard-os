# Companion mints scoped Bot tokens

Only the Companion (PIN) creates or revokes Bot tokens. A token names one Bot and is limited to specific Module types. Bots do not self-register. An unscoped token would let a weight Bot rewrite tasks.
