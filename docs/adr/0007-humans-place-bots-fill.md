# Humans Place tiles, Bots only fill them

A Bot publishes to an existing Module instance. If none exists for that type, the In API fails. Auto-creating instances would shove the household grid around. Layout mutation stays a later privilege, not the default.
